{
  "name": "hearthlight",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "lint": "eslint .",
    "test": "vitest",
    "test:unit": "vitest --run",
    "test:balance": "node scripts/bot-playtest.js --assert --ci",
    "balance:story": "node scripts/bot-playtest.js --story",
    "balance:baseline": "node scripts/bot-playtest.js --json > scripts/balance-baseline.json",
    "balance:compare": "node scripts/bot-playtest.js --compare scripts/balance-baseline.json",
    "test:quality": "npm run lint && npm run test:unit && npm run test:balance && npm run build",
    "test:smoke": "npm run build && node scripts/browser-smoke.mjs"
  },
  "dependencies": {
    "react": "^19.2.7",
    "react-dom": "^19.2.7"
  },
  "devDependencies": {
    "@eslint/js": "^10.0.1",
    "@vitejs/plugin-react": "^6.0.3",
    "eslint": "^10.6.0",
    "eslint-plugin-react-hooks": "^7.1.1",
    "eslint-plugin-react-refresh": "^0.5.3",
    "globals": "^17.7.0",
    "puppeteer-core": "^25.3.0",
    "vite": "^8.1.4",
    "vitest": "^4.1.10"
  }
}
